export interface Data {
    id: number;
    name: string;
  }

export interface DataJson {
  chart: {
    type: string,
    data: [
      {x: string, value: number, qtrBreakdown: [[string, number], [string, number], [string, number], [string, number]]},
      {x: string, value: number, qtrBreakdown: [[string, number], [string, number], [string, number], [string, number]]},
      {x: string, value: number, qtrBreakdown: [[string, number], [string, number], [string, number], [string, number]]},
      {x: string, value: number, qtrBreakdown: [[string, number], [string, number], [string, number], [string, number]]},
      {x: string, value: number, qtrBreakdown: [[string, number], [string, number], [string, number], [string, number]]},
      {x: string, value: number, qtrBreakdown: [[string, number], [string, number], [string, number], [string, number]]},
      {x: string, value: number, qtrBreakdown: [[string, number], [string, number], [string, number], [string, number]]},
      {x: string, value: number, qtrBreakdown: [[string, number], [string, number], [string, number], [string, number]]},
      {x: string, value: number, qtrBreakdown: [[string, number], [string, number], [string, number], [string, number]]},
      {x: string, value: number, qtrBreakdown: [[string, number], [string, number], [string, number], [string, number]]},
      {x: string, value: number, qtrBreakdown: [[string, number], [string, number], [string, number], [string, number]]},
      {x: string, value: number, qtrBreakdown: [[string, number], [string, number], [string, number], [string, number]]},
      {x: string, value: number, qtrBreakdown: [[string, number], [string, number], [string, number], [string, number]]},
      {x: string, value: number, qtrBreakdown: [[string, number], [string, number], [string, number], [string, number]]},
      {x: string, value: number, qtrBreakdown: [[string, number], [string, number], [string, number], [string, number]]},
      {x: string, value: number, qtrBreakdown: [[string, number], [string, number], [string, number], [string, number]]}
    ],
  }
}